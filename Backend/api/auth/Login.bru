meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
      "identifier": "tanvir123",
      "password": "{{password}}"
    }
}

settings {
  encodeUrl: true
  timeout: 0
}
